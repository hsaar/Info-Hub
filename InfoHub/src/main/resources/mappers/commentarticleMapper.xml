<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.infohub.project.commentMapper">

	<insert id="insert" parameterType="com.infohub.project.comment.CommentVO">
	    
	    INSERT INTO comment_article( created_date, last_modified, comment, hearts, login_login_id, article_article_id)
	    VALUES ( now(), now(), #{comment}, 0, 2, #{article_article_id})
	    
	</insert>
	
	
	<select id="listAll"  resultType="com.infohub.project.comment.CommentVO">
		SELECT c.* FROM comment_article c join article a on article_id = article_article_id
		WHERE article_article_id = #{article_article_id}
	
	</select>

</mapper>