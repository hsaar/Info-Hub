<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.infohub.project.loginMapper">

	<select id="listAll" resultType="com.infohub.project.login.LoginDTO">
		SELECT * FROM infohub.login
	</select>
	
	<select id="getUserById" resultType="com.infohub.project.login.LoginDTO">
		SELECT * FROM infohub.login where login_id = #{login_id}
	</select>
	
	<select id="getUserByUsername" resultType="com.infohub.project.login.LoginDTO">
		SELECT * FROM infohub.login where username = #{username}
	</select>
	
	<select id="login" resultType="com.infohub.project.login.LoginDTO">
		SELECT * FROM infohub.login where username = #{username} and password = #{password}
	</select>
	
	<select id="checkUsernameDuplicate" resultType="int">
		SELECT count(username) FROM infohub.login where username = #{username}
	</select>
	
	<select id="checkNameDuplicate" resultType="int">
		SELECT count(name) FROM infohub.login where name = #{name}
	</select>
	
	<insert id="insertUser">
		INSERT INTO infohub.login (username, password, name, email, phone, created_date, status, role, age) 
		VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, now(), 1, 1, #{age});
	</insert>
	
	<update id="updateUser">
		UPDATE infohub.login 
		SET password = #{password}, name = #{name}, email = #{email}, phone = #{phone}, age = #{age} 
		WHERE (username = #{username});
	</update>
	
	<update id="deleteUser">
		UPDATE infohub.login SET status = 0
		WHERE (username = #{username});
	</update>
	
	
	
</mapper>